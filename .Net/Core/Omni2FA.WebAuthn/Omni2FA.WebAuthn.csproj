<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>Omni2FA.WebAuthn</PackageId>
    <Description>Omni2FA — WebAuthn (passkeys, FIDO2 security keys) ceremony built on Fido2NetLib. Framework-agnostic — references no ASP.NET Core. Consumed by Omni2FA.AspNetCore.</Description>
    <PackageTags>2fa;mfa;webauthn;fido2;passkey;security-key;framework-agnostic</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Omni2FA.Core\Omni2FA.Core.csproj" />
  </ItemGroup>

</Project>
